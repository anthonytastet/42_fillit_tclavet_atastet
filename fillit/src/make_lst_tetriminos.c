#include "../libft/libft.h"
#include "../include/fillit.h"

t_tetrimino	make_lst_tetriminos(char **tab_tet)
{
	t_tetrimino	*lst_start;
	
	if ((lst_start = (t_tetrimino*)malloc(sizeof(*t_tetrimino))) == NULL)
		return (NULL);
	 
	
		
}
